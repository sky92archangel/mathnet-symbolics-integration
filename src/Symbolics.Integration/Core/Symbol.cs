namespace MathNet.Symbolics.Integration.Core;

/// <summary>
/// (EN) Represents a named symbol/variable. Structural equality by name.
/// (ZH) 表示具名符号/变量。按名称进行结构相等比较。
/// </summary>
public sealed record Symbol(string Name);
